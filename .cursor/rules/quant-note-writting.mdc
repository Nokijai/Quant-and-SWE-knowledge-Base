---
alwaysApply: true
---

每一篇筆記請嚴格遵守 「概念 -> 數學公式 -> 直觀解釋」 的結構，範例如下：
範例：02_cointegration_theory.md核心概念： 兩個不平穩的序列，其線性組合卻是平穩的。數學公式：如果序列 $X_t$ 和 $Y_t$ 都是 $I(1)$ 過程（即一階差分平穩），若存在係數 $\beta$，使得：$$Spread_t = X_t - \beta Y_t = \epsilon_t$$且 $\epsilon_t$ 為 $I(0)$（平穩），則稱 $X_t$ 與 $Y_t$ 協整。直觀解釋： 想像兩個人遛狗。$X_t$ 和 $Y_t$ 是這兩個人，狗鏈的長度 $\beta$ 就是我們尋找的參數。雖然兩個人走路的路徑都是隨機的，但因為狗鏈牽著，他們永遠不會離得太遠。這條「狗鏈」就是協整關係。
